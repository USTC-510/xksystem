<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.system.mapper.StudentMapper">
    <insert id="insertStudent">
        insert into student(name,id,gender,password,phonenumber,age,level) values(#{name},#{id},#{gender},#{password},#{phonenumber},#{age},#{level});
    </insert>
    <select id="selectAll" resultType="student">
        select * from student;
    </select>
    <select id="selectbyId" resultType="student">
        select * from student where id = #{username};
    </select>
    <select id="selectbyName" resultType="student">
        select * from student where name = #{name};
    </select>
</mapper>